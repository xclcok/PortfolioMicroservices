server:
  port: 8083

spring:
  cloud:
    config:
      enabled: false

  application:
    name: SERVICE-PROFILE

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/essabarprofile?createDatabaseIfNotExist=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
