server:
  port: 8089

spring:
  cloud:
    config:
      enabled: false

  application:
    name: SERVICE-PROFILE

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/portfolioprofile?createDatabaseIfNotExist=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
